name: Simple Scheduled Workflow

on:
  schedule:
    - cron: "0 9 * * *"   # Runs every day at 9:00 AM (UTC)

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello! This workflow is running on a schedule."
